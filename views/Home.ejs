<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            margin: 50px;
        }
        .container {
            max-width: 400px;
            margin: auto;
            padding: 20px;
            border: 1px solid #ddd;
            border-radius: 10px;
            box-shadow: 2px 2px 10px rgba(0, 0, 0, 0.1);
        }
        h1 {
            color: #333;
        }
        button {
            padding: 10px 20px;
            font-size: 16px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            margin-top: 10px;
        }
        .login {
            background-color: #007bff;
            color: white;
        }
        .logout {
            background-color: #dc3545;
            color: white;
        }
    </style>
</head>
<body>

    <div class="container">
        <h1>Welcome to My App</h1>

        <% if (isAuthenticated) { %>
            <p>Hello, <strong><%= user.name %></strong>!</p>
            <a href="/profile"><button class="profile">Profile</button></a>
            <a href="/token"><button class="token">Tokens</button></a>
            <a href="/silent-auth"><button class="token">Silent Authentication (Prompt=none)</button></a>
            <a href="/refresh-token"><button class="token">Refresh Token (/oauth/token)</button></a>
            <a href="/logout"><button class="logout">Logout</button></a>
        <% } else { %>
            <p>You are currently logged out.</p>
            <a href="/login"><button class="login">Login (Code Flow )</button></a>
        <% } %>
    </div>

</body>
</html>
