<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Home | My App</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      margin: 0;
      padding: 40px 20px;
      background-color: #f9fafb;
      color: #333;
    }

    .container {
      max-width: 900px;
      margin: auto;
    }

    h1 {
      font-size: 2rem;
      margin-bottom: 0.5em;
      text-align: center;
    }

    p {
      text-align: center;
      margin-bottom: 2em;
    }

    .user-info {
      margin-bottom: 2em;
      text-align: center;
    }

    .grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 1.5em;
      margin-bottom: 3em;
    }

    .card {
      background: white;
      border: 1px solid #e2e8f0;
      border-radius: 12px;
      padding: 1.2em;
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.06);
      transition: transform 0.2s ease;
      text-align: center;
    }

    .card:hover {
      transform: translateY(-4px);
    }

    .card button {
      padding: 10px 16px;
      font-size: 14px;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      background-color: #007bff;
      color: white;
    }

    .card.logout button {
      background-color: #dc3545;
    }

    .card a {
      text-decoration: none;
    }

    .section-header {
      font-size: 1.4rem;
      margin-bottom: 1em;
      text-align: center;
      color: #444;
    }

    .divider {
      border-top: 1px solid #ccc;
      margin: 4em 0 2em;
    }

    .footer-note {
      font-size: 0.9rem;
      color: #888;
      text-align: center;
    }
  </style>
</head>
<body>

  <div class="container">
    <h1>Welcome to My App</h1>

    <% if (isAuthenticated) { %>
      <div class="user-info">
        <p>Hello, <strong><%= user.name %></strong>!</p>
      </div>

      <div class="grid">
        <div class="card"><a href="/profile"><button>View Profile</button></a></div>
        <div class="card"><a href="/token"><button>Tokens</button></a></div>
        <div class="card"><a href="/login"><button>Interactive Login</button></a></div>
        <div class="card"><a href="/silent-auth"><button>Silent Auth</button></a></div>
        <div class="card"><a href="/refresh-token"><button>Refresh Token</button></a></div>
        <div class="card logout"><a href="/logout"><button>Logout</button></a></div>
      </div>
    <% } else { %>
      <p class="user-info">You are currently logged out.</p>
      <div class="grid">
        <div class="card"><a href="/login"><button>Login (Code Flow)</button></a></div>
      </div>
    <% } %>

    <div class="divider"></div>

    <h2 class="section-header">Developer Tools</h2>

    <div class="grid">
      <div class="card"><a href="/decode-jwt"><button>JWT Decoder</button></a></div>
      <div class="card"><a href="/decode-saml"><button>SAML Decoder</button></a></div>
      <div class="card"><a href="/base64"><button>Base64 Encoder/Decoder</button></a></div>
    </div>

    <p class="footer-note">Looking for more tools? Submit a feature request to the admin team.</p>
  </div>

</body>
</html>
